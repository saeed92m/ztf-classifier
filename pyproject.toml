[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ztf-classifier"
version = "0.4.0"
description = "Astronomical data analysis and discovery platform with ZTF scientific inference and provenance."
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = {text = "Proprietary"}
keywords = ["astronomy", "ZTF", "machine-learning", "astronomical-data-analysis", "scientific-discovery", "transient-classification"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Operating System :: POSIX :: Linux",
    "Topic :: Scientific/Engineering :: Astronomy",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

authors = [
    {name = "Alpha Team"}
]
maintainers = [
    {name = "Alpha Team"}
]

dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "matplotlib",
    "astropy",
    "astroquery",
    "alerce==2.3.1",
    "pyarrow",
    "scikit-learn",
    "joblib",
    "xgboost",
    "lightgbm",
    "shap",
    "requests",
]

[project.scripts]
ztf-classifier = "ztf_classifier.cli.main:main"
ztf-classifier-api = "ztf_classifier.api.server:main"
ztf-classifier-worker = "ztf_classifier.jobs.runner:main"

[project.optional-dependencies]
notebook = [
    "jupyterlab",
    "ipykernel",
]
web = [
    "fastapi>=0.141.1,<0.142",
    "uvicorn>=0.53.0,<0.54",
    "plotly",
    "streamlit",
]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "build",
    "pip-audit",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.setuptools.package-data]
ztf_classifier = ["web/static/*"]
